/* Placeholder — the virtual passport ships in Phase 6. */
export default function PassportPage() {
  return (
    <section className="flex flex-col items-start gap-s3">
      <h1 className="font-display text-2xl">Your first stamp awaits</h1>
      <p className="max-w-prose text-ink-soft">
        Every pretend booking earns a very real-looking stamp. Coming in Phase
        6.
      </p>
    </section>
  );
}
