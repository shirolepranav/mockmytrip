/* Placeholder — the trips dashboard ships in Phase 6. */
export default function TripsPage() {
  return (
    <section className="flex flex-col items-start gap-s3">
      <h1 className="font-display text-2xl">Your adventures await</h1>
      <p className="max-w-prose text-ink-soft">
        Booked trips, countdowns, and memories will live here from Phase 6.
      </p>
    </section>
  );
}
