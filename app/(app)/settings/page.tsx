/* Placeholder — full settings (motion, theme, export/delete) ship in Phase 8. */
export default function SettingsPage() {
  return (
    <section className="flex flex-col items-start gap-s3">
      <h1 className="font-display text-2xl">Settings</h1>
      <p className="max-w-prose text-ink-soft">
        Reduced motion, night-flight theme, notifications, and data
        export/delete land here across Phases 2–8.
      </p>
    </section>
  );
}
