/* Placeholder — the $0.00 checkout and boarding-pass reveal ship in Phase 5. */
export default function CheckoutPage() {
  return (
    <section className="flex flex-col items-start gap-s3">
      <h1 className="font-display text-2xl">Almost there</h1>
      <p className="max-w-prose text-ink-soft">
        Checkout — with your $0.00 total and a boarding-pass reveal — ships
        in Phase 5. Your trip is saved, so it&apos;ll be right here when it
        does.
      </p>
    </section>
  );
}
